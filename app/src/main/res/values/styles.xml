<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/cryptobox_blue</item>
        <item name="colorPrimaryDark">@color/cryptobox_blue_dark</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <!-- app splash screen image TODO add non stretchable logo -->
        <!--<item name="android:windowContentOverlay">@drawable/lockbox</item>-->
    </style>
</resources>
